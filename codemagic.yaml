workflows:
  ios-workflow:
    name: iOS App Store Build
    max_build_duration: 60

    environment:
      node: 22
      xcode: latest

    integrations:
      app_store_connect: NewKey   # use exatamente o nome da sua key

    scripts:
      - name: Install dependencies
        script: |
          npm install

      - name: Build web app
        script: |
          npm run build

      - name: Sync Capacitor
        script: |
          npx cap sync ios

      - name: Set up code signing
        script: |
          xcode-project use-profiles

      - name: Archive iOS
        script: |
          cd ios/App
          xcodebuild \
            -project App.xcodeproj \
            -scheme App \
            -configuration Release \
            -archivePath build/App.xcarchive \
            archive

    artifacts:
      - ios/App/build/**/*.xcarchive
